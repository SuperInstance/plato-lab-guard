[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-lab-guard"
version = "0.1.0"
description = "Lab guard — hypothesis gating, absolute quantifier detection, claim validation"
license = {text = "MIT"}
requires-python = ">=3.8"
